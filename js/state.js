/**
 * state.js - Shared application state
 * Manages the global save object that caches farm information
 */

export const save = {};

/**
 * Initialize save state
 */
export function initializeSaveState() {
  // This will be populated by parseSummary() when a save file is loaded
}
